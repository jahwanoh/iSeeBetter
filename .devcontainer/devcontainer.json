{
	"name": "bepro_srgan",
	"context": "..",
	// "dockerFile": "../Dockerfile.remote.arm64",
	"image": "srgan-dev:latest",
	"settings": {
		"terminal.integrated.shell.linux": null
	},
	"extensions": [
		"ms-vscode.cpptools",
		"ms-vscode.cmake-tools",
		"xaver.clang-format"
	],
	"forwardPorts": [
		8080
	],
	"runArgs": [
		"--runtime=nvidia",
		"--privileged",
		"--net=host",
		"--gpus=all",
		"--volume=/tmp:/tmp",
		"--volume=/var/log:/var/log",
		"--volume=/var/www/static:/var/www/static"
	],
	"workspaceFolder": "/SRGAN",
	"workspaceMount": "source=/home/research/SRGAN,target=/SRGAN,type=bind,consistency=cached",
	"remoteUser": "root"
	// "remoteEnv": {
	// 	"LOCAL_WORKSPACE_FOLDER": "${localWorkspaceFolder}"
	// }
}